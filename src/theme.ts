import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#e97826',
    },
    secondary: {
      main: '#7f7f7f',
    },
    background: {
      default: '#d9d9d9',
    },
  },
});

export default theme;
